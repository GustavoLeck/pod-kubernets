apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
spec:
  containers:
    - name: jplay-api
      image: node:20-alpine
      ports:
        - protocol: TCP
          port: 5050
          targetPort: 5050
      type: NodePort
